# Database (Neon serverless Postgres)
DATABASE_URL="postgresql://..."

# x402 payment settlement (get from cdp.coinbase.com)
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
X402_PAYEE_ADDRESS=0x...

# AWS (SES email sending + Route53 DNS for subdomains)
# Use a scoped IAM user with access only to the x402email.com hosted zone + SES
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
ROUTE53_HOSTED_ZONE_ID=

# App
NEXT_PUBLIC_BASE_URL=https://x402email.com
EMAIL_DOMAIN=x402email.com
